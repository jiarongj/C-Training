#include <iostream>
#include <list>
using namespace std;
int main(void){
    std::list<int> mylist;
    
    for (int i = 1; i != 11; i++)
        mylist.push_back(i);
    
    for (std::list<int>::iterator it = mylist.begin(); it != mylist.end(); it++)
        std::cout << *it << ' ';
}
